/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.apartup.com', 'entreprendas.es', 'www.visitvalencia.com'], 
    },
  }
  
  export default nextConfig;
  